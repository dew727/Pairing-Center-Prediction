"""Shared helper functions for the ZIM/HIM-8 pairing-center project.

This "src" folder holds the reusable building blocks (loading data, scanning for
motifs, building PWMs, finding orthologs, embeddings, and the scoring metric).
The scripts in the main project folder import from here so the logic lives in
one place. These files only define functions; they don't run anything on their
own.
"""
